# Preview assets: brownian-motion

`preview.png` (still) and `preview.mp4` (loop) to be captured by the
owner from a running instance of `/pieces/brownian-motion`. The
content frontmatter references both paths; until they exist the index
thumbnail will be empty (the `<img>`/`<video>` will simply not load).

Suggested capture: orbit slightly off-axis, simulation time
around t ≈ 4 s so the cloud is visibly diffuse but the origin remains
the densest point. Match the page background (`#FAFAF7`).
